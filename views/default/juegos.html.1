<head>
		<LINK REL=StyleSheet HREF="{{=URL('static','css/misStyles.css')}}" TYPE="text/css" MEDIA=screen> 
		<meta charset="utf-8">
		<title>A 'ND I GAMES</title>
</head>
<header>
		<a class="title">
		 A 'nd I GAMES 
		</a>
</header>
<body>


<!--
<ol>
        {{for i in jugadores:}}
        <li>  <b>{{=i[0]}}</li>
    {{pass}}
    </ol>-->
    {{
    
    nombre = request.vars.nombre
    bandera = False
    #verifica si el nombre de usuario ya existe, si no existe crea un nuevo registro en la tabla
    if(db.executesql("select * from Jugador where nombreJugador='"+nombre+"'")): bandera = True
    if(bandera==False):
        db.executesql("insert into Jugador (nombreJugador) values ('"+nombre+"')")
    }}
    {{pass}}
    <h1 style="color:white">
        Bienvenido '{{=nombre}}'
    </h1>
    <script>
        function redireccionar(opcion){
            if(opcion==1){
                window.open("second.html", "PROFES UD", "width=1800, height=1000")
            }
        }
    </script>
    <h4 style="color:white;text-align:center">
        JUEGOS DISPONIBLES
    </h4>
    <div>
        <input type='image' src="{{=URL('static','image.png')}}" onclick="redireccionar(1)">
    <div>
    <div id="tablaJuegos">
        <h3>
            JUEGOS MAS POPULARES
        </h3>
        {{cont=0}}
        {{for i in juegos:}}
        {{imagen=i[2]}}
        {{cont=cont+1}}
        </b><a href="{{=i[1]}}">
        {{imagen=i[2]}}
        <img class="direccion" src="{{=URL('static','imagenes')}}/{{=imagen}}" title="{{=i[0]}}">
        </a>
        {{if (cont%5==0):}}
        <br>
        {{else:}}
        {{pass}}
        {{pass}}
    </div>


</body>
